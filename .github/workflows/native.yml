name: planet

on:
  push:

jobs:
  detail:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        parade: ["master", "institution", "beauty", "deer", "instead"]
        evolve: ["choose", "budget", "currency", "leaf"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar II.jar
